# =============================================================================
#  وابستگی‌های محیط توسعه — آکادمی HSE
#  نصب:  pip install -r requirements.txt
# =============================================================================

# --- هسته ---
Django==5.2.17

# --- خواندن تنظیمات از فایل .env ---
django-environ==0.14.0

# --- کار با تصاویر (ImageField مدل‌ها) ---
pillow==12.3.0

# --- سرو کردن فایل‌های استاتیک (در Production کاربرد اصلی را دارد) ---
whitenoise==6.12.0

# --- تماس با سرویس‌های خارجی (پنل پیامک، استعلام هویت، درگاه پرداخت) ---
requests==2.34.2

# --- ساخت فایل PDF گواهی ---
# گواهی روی خود سرور ساخته می‌شود، نه با سرویس آنلاین: روزی که آن سرویس
# قطع باشد نباید صدور گواهی متوقف شود، و اطلاعات هویتی دانشجو هم از سرور
# بیرون نمی‌رود. سه کتابخانه پایین همگی پایتون خالص‌اند و روی ویندوز هم
# بدون کامپایل نصب می‌شوند.
reportlab==5.0.1

# --- QR روی گواهی (برای استعلام اصالت) ---
qrcode==8.2

# --- چسباندن حروف فارسی و چیدن راست‌به‌چپ در PDF ---
# موتور PDF خودش این دو کار را انجام نمی‌دهد؛ بدون اینها حروف فارسی جدا
# و برعکس چاپ می‌شوند.
arabic-reshaper==3.0.1
python-bidi==0.6.11
