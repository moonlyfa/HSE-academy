# =============================================================================
#  وابستگی‌های محیط توسعه — آکادمی HSE
#  نصب:  pip install -r requirements.txt
# =============================================================================

# --- هسته ---
Django==5.2.17

# --- خواندن تنظیمات از فایل .env ---
django-environ==0.14.0

# --- کار با تصاویر (ImageField مدل‌ها) ---
pillow==12.3.0

# --- سرو کردن فایل‌های استاتیک (در Production کاربرد اصلی را دارد) ---
whitenoise==6.12.0

# --- تماس با سرویس‌های خارجی (پنل پیامک، استعلام هویت، درگاه پرداخت) ---
requests==2.34.2
